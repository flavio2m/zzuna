Future<String> saveAndDownloadFileImpl({
  required List<int> bytes,
  required String fileName,
}) async {
  throw UnsupportedError(
    'Plataforma não suportada para salvamento de arquivo.',
  );
}
